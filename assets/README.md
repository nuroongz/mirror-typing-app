# Assets

이 폴더에는 앱 아이콘과 스플래시 이미지가 들어갑니다.

`app.json` 에서 다음 파일을 참조합니다:

- `icon.png` (1024x1024)
- `splash.png` (1242x2436 권장)
- `adaptive-icon.png` (1024x1024, Android)
- `favicon.png` (48x48, web)

Phase 1 단계에서는 이미지가 없어도 Expo Go 로 실행할 수 있습니다.
필요 시 `npx expo prebuild` 또는 빌드 단계에서 생성/추가하세요.
